x-base: &base
  restart: unless-stopped
  stop_grace_period: 1m

services:
  papermc:
    image: aceheliflyer/papermc:openjdk25
    volumes:
      - ./minecraft:/home/paper/minecraft
    ports:
      - '0.0.0.0:25565:25565/tcp'
    <<: *base
    stdin_open: true
    tty: true
